PORT=5000
TCP_PORT=4001

# --- Database (MySQL) ---
# LIVE DATABASE:
 DATABASE_URL="mysql://u963801592_bacelar_User:Bacelar%40123@srv1100.hstgr.io:3306/u963801592_bacelar_Dev"

# LOCAL DATABASE:
# Replace 'password' with your local MySQL password if it's different (leave blank if no password, e.g. root:@localhost:3306/bacelar)
# DATABASE_URL="mysql://root:@localhost:3306/bacelar"

JWT_SECRET="super-secret-jwt-key"
JWT_EXPIRES_IN="1d"
