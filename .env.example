PORT=3000
TCP_PORT=4001
EXAM_RESULT_TCP_PORT=4001
DATABASE_URL="mysql://root:password@localhost:3306/backend_db"
JWT_SECRET="super-secret-jwt-key"
JWT_EXPIRES_IN="1d"
RAZORPAY_KEY_ID="rzp_test_xxxxx"
RAZORPAY_KEY_SECRET="xxxxx"

# ICICI payment gateway (URL + secrets live only on backend)
# Testing placeholder for now — replace ICICI_PAYMENT_URL with live checkout URL.
ICICI_PAYMENT_URL="https://eazypay.icicibank.com/"

# LMS / library integration. Put real values only in the server env, never in source or the website.
MOODLE_BASE_URL=https://lms.bacelar.org
MOODLE_WS_TOKEN=
KOHA_BASE_URL=https://library.bacelar.org
KOHA_CLIENT_ID=
KOHA_CLIENT_SECRET=
KOHA_LIBRARY_ID=BAC
KOHA_STUDENT_CATEGORY=STUDENT
# Leave empty. A number here syncs up to 10 unsynced enrollments on that interval. It does not run a full bulk sync.
IDENTITY_SYNC_INTERVAL_MINUTES=
# ICICI_MERCHANT_ID=""
# ICICI_MERCHANT_KEY=""
